---
name: Yamllint workflows

on:  # yamllint disable-line rule:truthy
  push:
    branches: [main]
    paths: ['.github/workflows/**']

jobs:
  create-release:
    uses: obervinov/_templates/.github/workflows/test.yamllint.yml@main
